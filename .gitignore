# 构建产物
/build/
/.build/
DerivedData/

# Xcode 的用户级状态（窗口布局、断点、上次选中的 scheme）
xcuserdata/
*.xcuserstate

# macOS
.DS_Store

# 凭据类文件：现在没有，公开后一次误 add -A 就是事故
.env
.env.*
.dev.vars
*.p8
*.p12
*.pem
*.mobileprovision
*.cer

# 本机 agent 工作树
.claude/
